# Feedback ledger — flow-friction-audit

Each entry below is standalone. Do not edit or cross-reference prior entries — the ledger is an append-only log.

---

## 2026-07-24T00:00:00+01:00 — flow-friction-audit vunversioned

**Summary**
Olly ran flow-friction-audit against Plain's `support-app` staging to audit blank-slate onboarding of a new workspace, producing `friction-audit/onboarding-20260724/` — 10 named findings, step-log evidence, an `impeccable` critique, and nine wireframed fix directions across three pain points. The overall pipeline structure landed well. The problems were with how the output is shaped and where the human enters. `findings.md` opens straight into a ranked finding list with no top-line summary; the *problems* — the synthesised meaning of the evidence — only appear at the very bottom as "Main pain points routed to impeccable", after the design work is already done, so there was no point at which he could confirm or challenge them. One major finding (the dead first click on "Connect your channels") is more likely a defect or automation artifact than an intended-behaviour friction, which this skill isn't meant to catch. The design output over-reached: the wireframe board squeezed nine directions onto one canvas, repeated itself, and drifted from low-fi sketch into prototype, while the prior-art research that should ground those designs was an unnamed clause in stage 5 with no output the user could follow up on. The run also had to improvise a driver pivot mid-audit (Playwright's fresh Chromium hit the Google Workspace SSO wall → claude-in-chrome against the live session), which the skill doesn't document. Rated 60/100.

**Selected proposals**
- [high | structure] Restructure `findings.md` and the template as evidence → observations → problems: keep micro-findings as an observation layer, add a first-class `## Problems` section synthesising them into 2–4 named problems each citing the finding IDs that produced it, and delete the trailing "Main pain points routed to impeccable" duplicate — expected impact: the report argues from evidence to meaning in reading order, with one canonical statement of what's wrong instead of two at opposite ends of the file.
- [high | instructions] Add stage 3b "align the problems with the user" — a mandatory HITL gate between scoring and routing to impeccable: present the named problems with evidence, ask what's missing / disputed / mis-clustered, proceed only after alignment; state that stages 2–3 stay blind so the evidence is unbiased and the gate is the first point of human input — expected impact: the expensive half of the pipeline (critique + nine fix directions) is spent on problems the user has agreed are the real ones.
- [high | instructions] Add an explicit bugs-are-out-of-scope rule to stage 2 and Scope boundaries: the skill audits intended behaviour, not defects; re-test any anomaly on a fresh load before promoting it to a finding; intermittent anomalies go in a separate `## Possible bugs (out of scope, not audited)` list, never in the ranked findings — expected impact: stops browser flakiness and product defects being scored as UX friction and inflating severity (the `onboarding-connect-channels-dead-then-navigates` case).
- [high | references] Add `references/wireframe-style.md` defining stage 5 render fidelity: one direction per frame, one frame per screen at generous size (never a multi-direction grid); greyscale boxes/lines/placeholder rules only; no real colour, brand type, imagery or interactive-looking chrome; hand-written annotations and arrows for intent; every frame labelled with direction ID and the problem it solves; no repeated screens unless the diff is the point. Ship the designer's Next-steps sketch as `assets/wireframe-reference.png` as target fidelity and state the line: a wireframe communicates structure and intent — the moment it looks like a working screen it's a prototype and it's wrong — expected impact: legible, unsquashed, sketch-fidelity wireframes matching how the design team actually works, instead of a cramped pseudo-prototype board.
- [high | instructions] Require an explicit target URL at invocation and attach to the user's already-running browser session at that URL — never assume localhost, never spin up a dev server or a fresh browser profile — expected impact: the audit runs against the surface the user actually meant, with existing auth and state intact, instead of stalling on a login wall or auditing the wrong environment.
- [medium | structure] Add a required top-line summary as the first section of `findings.md` and the template: run goal, flow, one-paragraph verdict, the named problems as bullets, headline counts — expected impact: the report is scannable in 30 seconds and shareable without a reader assembling the story from a ranked list.
- [medium | instructions] Promote prior-art research to a named step inside stage 5 keyed to the aligned problems — research how well-regarded products solve each agreed problem before generating any direction, carry the precedent into `impeccable`, and require a closing `## Inspiration — where to look next` section in `findings.md` listing products/surfaces per problem with a one-line note on what each does right — expected impact: designs are grounded in precedent rather than invented, and the user leaves with an extendable research trail.
- [low | error_handling] Document the SSO/auth driver fallback in stage 1b: a fresh Playwright Chromium can't be scripted past enterprise SSO, so name driving the user's already-authenticated real browser session (claude-in-chrome) as the sanctioned fallback and require the report to state which driver was used — expected impact: removes the mid-audit stall this run hit and makes the evidence's provenance explicit.

**Discarded proposals**
- None — all proposals were selected.

---

## 2026-07-25T16:09:21+01:00 — flow-friction-audit vunversioned

**Summary**
Olly ran flow-friction-audit against Plain's `support-app` to audit blank-slate workspace onboarding a second time, then had the run's output independently evaluated rather than accepting the running agent's self-assessment. The five-stage pipeline held up and the changes from the previous round landed: the 3b alignment gate materially reshaped the problem set rather than rubber-stamping it, the bugs-out-of-scope rule correctly kept a dead click and a transient asset failure out of the scored findings, and the problems-first report structure read conclusion-first. The strongest work in the run came from something the skill never asked for — reading the audited product's source, which produced three mechanism-level findings that verified line-for-line against `useNextStepsChecklist.ts` and `useWorkspaceIsEmpty.ts` and answered a question that would otherwise have required mutating a live workspace. The failures were mostly about where the run's output lives and how honestly it is framed. Artifacts landed in the invoking repo rather than the audited product's, and the twelve wireframes registered in Open Design three times over under epoch-stamped namespaces with inferred manifests, so they were never actually viewable in the design tool they were rendered for. Two of the twelve frames shipped with annotation text overlapping into illegibility because notes are positioned by hand. The critique ran inline against its own spec and landed in a separate file that nothing in the report connects to. Precedent research came from aggregator listicles and carried an unsourced vendor statistic into the deliverable. Fifteen screenshots dominated the run's context cost while only two were persisted, so the visual record was expensive and absent at the same time. And the run discovered mid-flight that it was a re-run, which prompted the clearest steer of the session: runs should be fresh by default, not diffed against stale prior audits unless the user asks. Rated 70/100.

**Selected proposals**
- [high | instructions] Invert prior-run detection into fresh-run isolation: never search for, read, or surface prior friction-audit artifacts; each invocation scopes, walks and scores from scratch, and cross-run comparison becomes an explicit opt-in the user requests — expected impact: no stale findings or prior fix directions leak in to dilute the run or become the ceiling on new design work; the report states what is true today.
- [high | structure] Pin the artifacts directory to `<audited-repo>/.claude/friction-audit/<flow>-<YYYYMMDD>/` rather than the invoking working directory — expected impact: audits live next to the product they audit regardless of where the agent was invoked from.
- [high | references] Document why Open Design artifacts are registered but not findable, and how to fix it: the web UI runs on a different port from the daemon (daemon+1 locally, both on 7456 packaged), re-registering each render pass under a new epoch namespace leaves three copies with nothing marking the current one, and omitting `--manifest` makes the UI list raw paths instead of titles; plus `--input` copies rather than references, so the run directory and the OD copy diverge on edit — expected impact: wireframes are assessed inside the design tool instead of as loose files, once each, under one run-named namespace.
- [high | structure] Delete the out-of-scope defects section from `findings.md` and the template; anomalies are mentioned to the user in conversation and never enter the report, as a finding, as evidence inside one, or as a section — expected impact: the report carries only intended-behaviour design findings, with nothing competing for the reader's attention.
- [high | instructions] Add stage 3a, corroborate against source: read the components behind each finding to find root causes and resolve open questions without touching live state, and state no claim the code doesn't support — expected impact: observations become mechanisms, and claims stop being rounded up beyond what the source shows.
- [high | instructions] State a live-state mutation policy in stage 2: never notify a third party or spend money; genuinely reversible UI-state actions are free and need no permission; answer from source where only an irreversible action would do; name the steps never exercised in the report — expected impact: removes repeated ad-hoc judgement calls mid-walk without letting a blanket no-touch posture leave the central mechanism unobserved.
- [high | instructions] Replace screenshot evidence with citation evidence: drop the save-each-snapshot instruction and screenshots from the artifact list entirely, and require every finding to cite its step-log row(s) plus `file:line` for anything read from source — expected impact: findings stay traceable without images, and the run's dominant context cost disappears.
- [high | instructions] Add an execution-shape section: source corroboration and per-problem precedent research fan out to parallel subagents, `impeccable critique`'s two assessments run isolated as a correctness requirement, wireframe authoring stays single-context, and the parent remains the synthesis point for the 3b gate — expected impact: fixes the shallow research and non-independent critique that serial execution produced, without breaking the single-context evidence set the gate depends on.
- [high | structure] Fold the critique into `findings.md` as a `## Critique` section and require each fix direction to name the critique point it answers — expected impact: the critique's emphasis visibly drives the design directions instead of sitting in a separate file nobody opens.
- [high | references] Add annotation placement mechanics to `wireframe-style.md`: reserve a margin column, stack notes in document flow so heights are measured rather than assumed, anchor arrows to elements, never run a connector across the content it describes, and read every rendered frame — expected impact: no overlapping illegible annotations, which are invisible in the markup and only appear in the render.
- [medium | instructions] Deepen precedent research: at least one primary source per problem, name the specific surface examined, two or three worked examples per problem, and no statistic without a primary source — expected impact: fix directions argue from evidence rather than listicle summaries and vendor marketing claims.
- [medium | instructions] Require each finding to belong to exactly one problem, and re-cluster when one seems to belong to three — expected impact: cluster boundaries become testable instead of overlapping.
- [low | structure] Don't persist generator scripts as run artifacts, and use one run-named Open Design namespace instead of ad-hoc ones — expected impact: the run directory holds deliverables only and the design project stops accumulating clutter.

**Discarded proposals**
- [high | structure] Add out-of-scope and re-run-delta sections to the findings template with a status enum and tied counts — expected impact: would have made the improvised delta table accurate; discarded because runs are now fresh by default and defects don't belong in the report at all, so neither section should exist.
- [medium | instructions] Add a mechanical convergence check listing prior direction IDs and stating overlap per new direction — expected impact: would have caught the run overstating novelty; discarded because fresh-run isolation forbids reading prior directions in the first place.
- [medium | references] Clarify in `rubric.md` that a step which cannot complete for the target segment is a blocker rather than a major — expected impact: would have corrected a reported "0 blockers"; discarded because browser-driven severity is a signal rather than a precise measure.
- [medium | instructions] Apply the bugs-out-of-scope test at evidence level as its own rule — discarded as a standalone proposal; its substance is carried by one clause of the report-wide defect exclusion above.
- [low | error_handling] Note as a standalone rule that stage 5's handoff to impeccable may degrade when sub-agents are unavailable — discarded as standalone; carried inside the execution-shape proposal, which requires the report to say so and to state that agreement is not corroboration.

---

## 2026-07-26T12:08:38+01:00 — flow-friction-audit vunversioned

**Summary**
Third feedback round on flow-friction-audit, reviewing the 0726 run against Plain's `support-app` self-serve onboarding — and, unusually, reviewing Olly's own first diagnosis of that run adversarially before proposing anything. Every high-priority proposal from the previous round shipped and is visibly working: the report reads problems-first with a 30-second summary, the 3b alignment gate produced four genuinely synthesised problems rather than a repackaged finding list, 3a source corroboration turned observations into named mechanisms (`hasConnectedChannels` counting threads, `MIN_USERS_TO_COMPLETE = 3`, `toChannelKeys` seeding `'api'`), the critique is folded in as a `## Critique` section with dual-agent isolation stated, precedent research is primary-sourced with an explicit section refusing an unsourced vendor statistic, defects stayed out of the report, and artifacts landed in the audited repo rather than the invoking one. The evidence half of the pipeline is essentially done. Every remaining defect sits downstream of the 3b gate and shares one cause: stage 5 diverges to a fixed 3-per-problem quota and never converges before committing. Twelve directions were fully researched, sharpened by `impeccable shape`, and wireframed — and only then did the report's own trailing `Cross-cutting` section reveal that two pairs of directions reduce to the same thesis, and that "the cheapest true thing is not one of the twelve", the real blocker living in a component no direction owns. The skill discovered its own filler after paying to argue and draw all of it, then buried its recommendation in a trailing bullet. Olly's initial diagnosis blamed the wireframes and was argued down: the frames faithfully rendered what they were given, drawing is the cheapest stage in stage 5, and selecting on pitch-level sketches would gate on exactly the information the research exists to produce — systematically buying seductive ideas over tractable ones. The fix is a convergence gate after research and before drawing, plus a cheaper design-budget gate at 3b using information the user already holds. Separately, the team's Q3 growth metrics now exist and the audit never sees them, proposing no success signal for any of the twelve directions; notably the roadmap's own definition of *valuable set-up* (2 channels, 1+ workflow, 1+ Sidekick tool, 3+ members) is exactly the configuration-as-finish-line the audit's P1 attacks — produced blind, which is precisely the constraint on how metrics may enter. Rated 75/100.

**Selected proposals**
- [high | instructions] Add a convergence gate to stage 5, between research and wireframes: promote the trailing `Cross-cutting` analysis into a required stage that runs after the per-problem agents return prose and before anything is drawn, presented as the selection surface — which theses collapse into one, what composes vs. what is mutually exclusive, a recommended bundle, and as a required field the cheapest intervention that repairs the walked blocker whether or not it is one of the directions — then ask which directions get wireframed; `impeccable shape` sharpening stays before the gate, since its corrections are information the gate needs — expected impact: duplicates die before costing a research budget and a frame, the escape-hatch finding becomes reliable rather than incidental, and review attention is spent only on chosen directions, with net report addition of roughly zero because `Cross-cutting` moves rather than duplicates.
- [high | instructions] Extend 3b to allocate design budget across problems — one additional alignment question covering which problems get design work and which is load-bearing, since the 0726 problems were not peers (P3 is plausibly a symptom of P1) yet all four drew three directions each — expected impact: the expensive stage is scoped using cheap information the user already holds, before any research runs; highest leverage change in the batch, at one bullet in `references/alignment-doc.md` and one clause in SKILL.md 3b.
- [high | instructions] Make the direction count an output of selection rather than a constant: keep "three divergent ideas per problem" as the generation target — the independently-invented duplicates prove the blind fan-out worked — but stop treating it as the output contract, including the hardcoded "three divergent directions per problem" in `assets/findings-template.md` — expected impact: removes the 12-frame quota that manufactures filler without weakening coverage during generation.
- [high | instructions] Surface the recommendation and the sequencing: require the convergence gate's output to lead with the recommended bundle and an order (ship-now / this quarter / rewrite) drawn from the per-direction cost notes that already exist, instead of ending in an unranked menu of twelve mutually-exclusive structural rewrites framed as "alternatives, not a backlog" — expected impact: the deliverable ends in a decision rather than a decision to be made.
- [medium | instructions] Reconcile stage 5's closing "report what you handed off and what came back, don't editorialise a second critique on top" clause with the new recommendation requirement — read literally, that line is what produced the unranked menu and will silently cancel the recommendation proposal, so it needs one clause distinguishing "don't re-argue the critique" from "do state a recommendation" — expected impact: the recommendation actually lands instead of being undone by an instruction eleven stages away, at a cost of about one line.
- [medium | references] Bring the team's goal metrics in at 3b as business context, fetched and never cached: add the Q3 Roadmap URL to `references/alignment-doc.md` as the pointer to current goal metrics, fetched at the 3b gate, with the definitions never persisted in the skill (quarterly, Plain-specific, no staleness signal, so a stale metric reading as authoritative would silently reframe an audit) and never fetched at stage 1 (injecting metrics before the walk destroys the blindness stages 2–3 depend on — given the *valuable set-up* definition at scope, P1 would never have existed); metrics enter after the problems exist, as context feeding the 3b design-budget allocation rather than a scoring rubric, and where the evidence contradicts what a metric assumes the run says so plainly in the problem statement while leaving the metric itself to the team — expected impact: problems are weighted by what the business is trying to move, and a metric resting on a false assumption gets named rather than quietly designed around, without the audit drifting into an OKR proposal.
- [medium | instructions] Every fix direction names the signal that would prove it worked — one clause in stage 5's fan-out unit, since the report's sharpest observation is that the activation metric measures configuration rather than use, and it then proposes twelve changes with no instrumentation for any of them — expected impact: directions become shippable experiments rather than proposals, pairing naturally with the roadmap's funnel and closed-thread-count metrics.

**Discarded proposals**
- None of the seven surfaced proposals were discarded. Four items were argued down before reaching the list and must not be reintroduced: (1) `ideate` as a third mode — a mode boundary buys exactly one thing, surviving an asynchronous handoff, which 3b earned because team alignment takes days and needs a Notion page, whereas reading a dozen theses and picking four is a same-session decision; a third mode would cost a third artifact contract, a third re-entry-context spec and a three-way routing rule in a description already doing heavy lifting. Mode boundaries are for async handoffs; gates are for synchronous decisions. (2) A "raw ideas / sketches" artifact format — each direction already opens with a one-line `*Thesis:*`, which is the sketch layer; present the theses plus the convergence read at the gate. (3) Selecting ideas before the research runs — gates on exactly the information the research exists to produce, and buys seductive ideas over tractable ones (`p3-3C` reads as the most exciting idea in the doc and only research reveals its structural core is wrong and mostly outside the repo). (4) A `references/metrics.md` holding the Q3 definitions — bloat plus guaranteed staleness; pointer, not copy.

**Found while verifying, not proposed**
Running the trigger eval after the edits exposed an unrelated defect: the description undertriggered badly whenever no URL was in the prompt (positives 5/13, negatives 13/13). "why are people dropping off on the checkout page? can you actually go through it live" scored 0/3 despite the description already carrying the near-verbatim phrase "why do people drop off on this page" — so the phrase list was never the lever; the missing URL was. Claude asked for the URL *without* consulting the skill, meaning stage 1 happened outside the skill and the walk that followed need not follow its method. Fixed with one clause stating that settling the goal, flow and environment is the skill's own first step, so it triggers before a URL or credentials exist: positives 5/13 → 12/13 with negatives holding at 13/13. Residual known miss: a `shape`-mode entry naming an alignment doc by title rather than URL (1/3) — left unfixed rather than shipping an unmeasured description edit. Note for future rounds: `skill-creator`'s `run_eval.py` counts a trigger only when its own stub command name is invoked, so with this skill installed globally every genuine trigger reads as a miss — the first run returned 13/26 with every positive *and* every negative at 0/3, which is the signature of that bug rather than a real score. Patch the detection to accept the real skill name before trusting any number.

**Constraint on the update**
The skill is at 156 lines of SKILL.md plus five references and is already dense. This batch lands in roughly 15 net new lines across `SKILL.md`, `references/alignment-doc.md` and `assets/findings-template.md`. The convergence gate is a move of existing `Cross-cutting` content, not an addition. No new reference file for any item in this batch. Any proposal that cannot be expressed in two or three sentences at the existing register — imperative, reasoned, no restatement — is the wrong proposal.

